export enum EEntityGroupType {
    Events = 'events',
    Categories = 'categories',
}

export enum EInputType {
    Text = 'text',
    Color = 'color',
    Date = 'date',
    Time = 'time',
    Select = 'select',
}

export enum EResponseStatus {
    Success = 'success',
    Loading = 'loading',
    Error = 'error',
    Default = 'default',
}
